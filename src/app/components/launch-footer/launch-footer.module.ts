import { NgModule } from '@angular/core';
import { LaunchFooterComponent } from './launch-footer.component';

@NgModule({
	declarations: [
		LaunchFooterComponent,
	],
})
export class LaunchFooterModule { }
